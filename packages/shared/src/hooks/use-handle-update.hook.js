import { useEffect } from "react"
import { handleUpdate } from "../helpers"

function useHandleUpdate(version, appType) {
  // useEffect(() => {
  //  handleUpdate(version, appType)
  // }, [version, appType])
}

export { useHandleUpdate }
